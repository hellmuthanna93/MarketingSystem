---
description: Anna Hellmuth marketing brand — use DESIGN.md tokens, no hardcoded colors
globs: brand/**/*,styleguide/**/*,website/**/*,projects/**/*
alwaysApply: false
---

# Marketing brand (Anna Hellmuth)

Before styling or copy changes, read **[brand/DESIGN.md](brand/DESIGN.md)**.

- Edit colors only in `brand/tokens.css`
- Link `brand/tokens.css`, `typography.css`, `components.css` from sub-projects — never duplicate hex
- Burgundy `#64010d`, cream `#f7eae3`, text `#5c020c`; headings Libre Baskerville; body freight-text-pro
- Primary CTA: **Schedule your discovery call**
- `projects/table/` is legacy orange palette — not canonical website brand
